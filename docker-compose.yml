version: '3'

services:
  webapp:
    build:
      context: ./webapp
      dockerfile: Dockerfile
    ports:
      - "80:5000"
    depends_on:
      - redis
    environment:
      - FLASK_APP=app.py

  redis:
    image: "redis:latest"
